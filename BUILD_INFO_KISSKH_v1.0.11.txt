KissKH v1.0.11 runtime artifact validated for VUEO deployment.

Changes:
- Keeps v1.0.10 exact-title selection logic unchanged.
- Reuses the video kkey from a global runtime cache for up to 6 hours.
- Shares one in-flight key request across concurrent provider calls.
- Clears and refreshes the cached key once when the source endpoint returns HTTP 401/403.

Validation:
- manifest.json valid, KissKH version 1.0.11.
- providers/kisskh.js syntax valid.
- providers/kisskh.js and src/kisskh/index.js are byte-identical.
- Mock regression: concurrent calls -> one remote key request.
- Mock regression: later call -> cache-hit.
- Mock regression: rejected cached key -> one refresh then stream succeeds.

Note:
The runtime provider is self-contained. esbuild is not installed in this workspace, so the already-synchronized runtime provider was packaged directly.
